<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fengwo.module_chat">

    <application
        android:name=".mvp.ui.activity.debug.ChatApp"
        android:allowBackup="true"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <activity android:name=".mvp.ui.activity.social.userpage.UserSocialPageActivity"></activity>
        <activity android:name=".mvp.ui.activity.social.AddressBookActivity" />
        <activity android:name=".mvp.ui.activity.publish.PublishTwoActivity" />
        <activity android:name=".mvp.ui.activity.message.MessagesActivity" />

        <meta-data
            android:name="design_width"
            android:value="750" />
        <meta-data
            android:name="design_height"
            android:value="1334" />

        <activity android:name=".mvp.ui.activity.debug.DebugActivity" />
        <activity android:name=".mvp.ui.activity.debug.DebugHomeActivity">
      <!--      <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity
            android:name=".mvp.ui.activity.SocialSearchActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait" />

        <activity
            android:name=".mvp.ui.activity.social.ChatActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>