<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fengwo.module_live_vedio">

    <application
        android:name=".mvp.ui.activity.debug.LiveApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">

        <meta-data
            android:name="design_width"
            android:value="750"/>
        <meta-data
            android:name="design_height"
            android:value="1334"/>

        <activity android:name=".mvp.ui.activity.debug.DebugActivity">
         <!--   <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity android:name=".mvp.ui.activity.debug.DebugHomeActivity" />
        <activity android:name=".mvp.ui.activity.RankTuhaoActivity" />
        <activity android:name=".mvp.ui.activity.RankTopActivity" />
        <activity android:name=".mvp.ui.activity.RankPkActivity" />
        <activity android:name=".mvp.ui.activity.ZhuboDetailActivity" />
    </application>

</manifest>
